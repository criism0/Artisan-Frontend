version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "🔧 Usando Node.js 20 para compatibilidad con react-router-dom@7"
        - nvm install 20
        - nvm use 20
        - corepack enable
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
